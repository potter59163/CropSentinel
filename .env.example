# Google Maps JavaScript API key (Maps JS + optional Elevation API).
# Restrict by HTTP referrer in Google Cloud Console before any public deploy.
VITE_GOOGLE_MAPS_API_KEY=your_key_here

# Google Maps Map ID for vector/styled maps (optional but recommended for AdvancedMarker).
VITE_GOOGLE_MAPS_MAP_ID=
